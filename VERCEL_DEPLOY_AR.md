# دليل النشر على Vercel

## خطوات النشر السريع

### 1. رفع الكود إلى GitHub

إذا لم تكن قد فعلت ذلك بعد:

```bash
git init
git add .
git commit -m "Initial commit"
git remote add origin YOUR_GITHUB_REPO_URL
git push -u origin main
```

### 2. الربط مع Vercel

1. افتح [vercel.com](https://vercel.com)
2. سجل دخول باستخدام حساب GitHub
3. اضغط على "Add New Project"
4. اختر المستودع الذي رفعت عليه الكود

### 3. إعدادات المشروع

Vercel سيكتشف تلقائيًا أن المشروع يستخدم Vite وسيقوم بإعداد:

- **Build Command**: `npm run build`
- **Output Directory**: `dist`
- **Install Command**: `npm install`

لا تحتاج لتغيير أي شيء!

### 4. إضافة متغيرات البيئة (اختياري)

إذا كنت تريد تعيين مفتاح Gemini API:

1. اذهب إلى "Settings" في لوحة تحكم Vercel
2. اختر "Environment Variables"
3. أضف متغير جديد:
   - **Name**: `GEMINI_API_KEY`
   - **Value**: مفتاح API الخاص بك من https://aistudio.google.com/app/apikey

ملاحظة: يمكنك أيضًا إضافة مفتاح API من داخل التطبيق في صفحة الإعدادات.

### 5. النشر

اضغط على "Deploy" وانتظر بضع دقائق!

## ما تم إصلاحه

✅ إنشاء ملف `index.css` المفقود
✅ إضافة ملف `vercel.json` للتوجيه والتخزين المؤقت الأمثل
✅ إضافة تبعيات TypeScript المطلوبة (`@types/react`, `@types/react-dom`)
✅ تحديث `.gitignore` لمنع رفع ملفات غير ضرورية
✅ إنشاء `.vercelignore` لتحسين عملية البناء
✅ إنشاء `.env.example` كمرجع للمتغيرات البيئية

## استكشاف الأخطاء

### إذا فشل البناء:

1. **تحقق من السجلات (Logs)** في لوحة تحكم Vercel
2. **تأكد من أن الكود يعمل محليًا**:
   ```bash
   npm install
   npm run build
   ```
3. **تحقق من إصدار Node.js**: يجب أن يكون 18 أو أحدث

### إذا لم يعمل التطبيق بعد النشر:

1. تحقق من Console في المتصفح (F12)
2. تأكد من إضافة مفتاح API في الإعدادات داخل التطبيق
3. تحقق من أن جميع الملفات تم رفعها بشكل صحيح

## إعادة النشر التلقائي

Vercel سيقوم تلقائيًا بإعادة بناء ونشر التطبيق عند:
- عمل Push جديد إلى الـ branch الرئيسي (main/master)
- دمج Pull Request

## روابط مفيدة

- [وثائق Vercel](https://vercel.com/docs)
- [وثائق Vite](https://vitejs.dev/guide/)
- [الحصول على Gemini API Key](https://aistudio.google.com/app/apikey)

---

الآن تطبيقك جاهز للنشر! 🚀
